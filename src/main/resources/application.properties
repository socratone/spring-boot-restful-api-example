spring.application.name=spring-boot-restful-api

# h2 설정
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:testdb

# 데이터베이스가 생성된 이후에 data.sql이 실행되도록 한다.
spring.jpa.defer-datasource-initialization=true